% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{cap_fit}
\alias{cap_fit}
\alias{hdcap}
\alias{lcap}
\alias{mcap}
\alias{coc}
\alias{coc_d1}
\alias{capmediation}
\alias{hcap}
\alias{cappcl}
\title{Fit a CAP-family method}
\usage{
hdcap(...)

lcap(...)

mcap(...)

coc(...)

coc_d1(...)

capmediation(...)

hcap(...)

cappcl(...)
}
\arguments{
\item{...}{arguments passed to the underlying method estimator, e.g.
\code{stop.crt} (\code{"nD"} or \code{"DfD"}), \code{nD}, \code{DfD.thred}, \code{cov.shrinkage}. See
\code{vignette("CAPmethods")} for each method's full argument list.}
}
\value{
The fitted-model object from the underlying estimator: a list whose
elements depend on the method — typically the loading direction(s) \code{gamma},
coefficients (\code{beta} and/or \code{alpha}), per-subject variance \code{score}s, and
direction-selection diagnostics (\code{DfD}).
}
\description{
Thin wrappers that run each covariate-assisted principal (CAP) regression
method. Each forwards its arguments to the underlying estimator (sourced into
a private environment at package load, so the methods' identically named
internal helpers do not collide) and returns that estimator's result
unchanged.
}
\details{
\itemize{
\item \code{hdcap()} — high-dimensional CAP; \code{cov.shrinkage = FALSE} recovers classical CAP.
\item \code{lcap()} — longitudinal CAP with a time-invariant projection.
\item \code{mcap()} — multilevel CAP with cluster-varying loadings.
\item \code{coc()} — covariance-on-covariance regression (multi-direction selection);
\code{coc_d1()} estimates a single direction.
\item \code{capmediation()} — covariance-mediator analysis.
\item \code{hcap()} — high-dimensional-covariate CAP (sparse, \code{glmnet}-based).
\item \code{cappcl()} — clustering of covariance patterns.
}

Each method has a matching \link{cap_examples} generator producing ready-to-use
data; \code{vignette("CAPmethods")} shows a worked, plotted run of every method.
}
\examples{
d   <- hdcap_example(n = 30, Ti = 30)
fit <- hdcap(d$Y_list, d$X, nD = 1, cov.shrinkage = FALSE)
round(as.numeric(fit$beta), 2)
}
\seealso{
\link{cap_examples} for matching example data, \link{cap_boot} for bootstrap
inference, and \link{cap_internal} to reach any internal function by name.
}
